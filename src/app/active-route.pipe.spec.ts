import { ActiveRoutePipe } from './active-route.pipe';

describe('ActiveRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
